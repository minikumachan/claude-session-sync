# Normalize line endings across platforms
* text=auto eol=lf

# Shell scripts MUST be LF (CRLF breaks the shebang on macOS/Linux)
*.sh text eol=lf

# PowerShell is fine with CRLF on Windows
*.ps1 text eol=crlf
